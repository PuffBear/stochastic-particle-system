# Paper A compatibility path

The complete active project contract is maintained at `../paper/PROJECT.md`.
This compatibility path exists so the research-program validator can recognize
the repository's earlier single-paper layout. The active question and claims
must not diverge from the canonical `paper/` files.
